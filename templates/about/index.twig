{% extends "_layouts/main" %}

{% block content %}

{# feature image #}
<img src="{{ entry.featureImage.one().getUrl() }}" class="feature-img">

<section id="about">
  <h1 class="about-title">About Us</h1>

  <div class="about-content">
    {{ entry.body }}
  </div>
</section>

{% endblock %}
