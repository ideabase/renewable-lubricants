{% extends "_layout" %}



{% block content %}

<h1>Go with the best. Protect the environment</h1>


  {% for image in entry.productImage.limit(1) %}
    <img src="{{image.url}}">
  {% endfor %}

<p>{{entry.body}}</p>

<hr>

<div>
  <h3>Available Grades / Sizes</h3>
  <ul>
    <li>8 oz.
      {% for file in entry.files %}
        <a href="{{file.url}}">SDS Sheet (PDF)</a>
      {% endfor %}
    </li>
    <li>16 oz.
      {% for file in entry.files %}
        <a href="{{file.url}}">SDS Sheet (PDF)</a>
      {% endfor %}
    </li>
  </ul>
  <button>Add To Cart>></button>
</div>

<hr>

{# need javascript to switch between the different tabs #}
<div>
<a href"#">Specifications</a>
<a href"#">Reviews</a>
<a href"#">The Renewable Advantage</a>

</div>

<div class="related-products">
{# how to find related products? same type of product, similar price? #}

</div>


{% endblock %}
