{% extends "_layouts/main" %}

{% block content %}

<h1>{{ entry.title }}</h1>
<p>"{{ entry.quote }}"</p>
<p>– {{ entry.testimonialAuthor }}</p>
<p>{{ entry.authorDescription }}</p>

{% endblock %}
