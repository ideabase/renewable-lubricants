{# news and blog page #}
{% extends "_layouts/main" %}


{% block content %}

<section>
  <h1>Latest News</h1>
  <p>Read our latest news articles regarding our products, recent publications, and sponsorships below:</p>

  <ul>
    {% for entry in craft.entries.section('news').limit(3).all() %}
      <li>
        <a href="{{ entry.url }}">
          {% if entry.featureImage|length %}
            <img src="{{ entry.featureImage.one().getUrl("newsFeatureImage") }}">
          {% endif %}

          <h2>{{ entry.title }}</h2>

          {% if entry.description|length %} {# TODO: fix slice #}
            {% if entry.description|length > 50 %}
              <p>{{ entry.description|slice(0,50) }}</p>
            {% else %}
              <p>{{ entry.description }}</p>
            {% endif %}
          {% endif %}
        </a>
      </li>
    {% endfor %}
  </ul>

  <a href="/news">View All News</a>
</section>

<section>
  <h1>Blog Posts</h1>
  <p>Check out the latest blog posts regarding content ranging from videos on how to use our products to informational updates about the company.</p>

  <ul>
    {% for entry in craft.entries.section('blog').limit(3).all() %}
        <li>
          <a href="{{ entry.url }}">
            {% if entry.featureImage|length %}
              <img src="{{ entry.featureImage.one().getUrl("newsFeatureImage") }}">
            {% endif %}

          <h2>{{ entry.title }}</h2>

          {% if entry.description|length %} {# TODO: fix slice #}
            {% if entry.description|length > 50 %}
              <p>{{ entry.description|slice(0,50) }}</p>
            {% else %}
              <p>{{ entry.description }}</p>
            {% endif %}
          {% endif %}
        </a>
      </li>
    {% endfor %}
  </ul>

  <a href="/blog">View All Blog Posts</a>
</section>
{% endblock %}
